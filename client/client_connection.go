package zooweeper

// ClientConnection manages the socket i/o for the client. ClientConnection maintains a list of available servers to
// connect to and "transparently" switches servers it is connected to as needed.
type ClientConnection struct {
}
