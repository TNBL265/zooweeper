package zooweeper

// ZNode contains the data for a node in the data tree.
// A data node contains a reference to its parent, a byte array as its data, an  array of ACLs, a stat object,
// and a set of its children's paths.
type ZNode interface {
}
